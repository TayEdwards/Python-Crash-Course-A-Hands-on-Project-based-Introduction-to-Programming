personsName = " Fank!\t\nBob     "
message = f"{personsName}"

print(personsName)
print("")
print(personsName.strip()) # Removes whitespace on both ends
print(personsName.lstrip()) # Removes whitespace on the left only
print(personsName.rstrip()) # Removes whitespace on the right only

